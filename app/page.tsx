import React from 'react';
import LandingPage from '@/app/components/LandingPage';

export const metadata = {
  title: 'UPIEdge | Direct-to-Bank UPI Payment Gateway (0% Commission)',
  description: 'Accept instant UPI payments directly into your bank account with automated webhook verification and 0% gateway transaction cut.'
};

export default function RootLandingPage() {
  return <LandingPage />;
}
